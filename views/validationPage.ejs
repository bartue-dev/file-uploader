<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/output.css">
  <title>Opps</title>
</head>

<body>

  <div class="bg-white text-gray-900 font-sans container mx-auto px-6 py-8 max-w-lg">
    <h1 class="text-red-600 text-2xl font-bold mb-4">Oops! An Error Occurred</h1>
    <% if (errors) { %>
      <% errors.forEach(err=> { %>
        <p class="text-base">
          - <%= err.msg %>
        </p>
        <% }) %>
          <% } %>

            <a class="link link-info" onclick="window.history.back()">back</a>
  </div>


</body>

</html>